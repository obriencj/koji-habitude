# Template that generates multiple YAML documents
---
type: template
name: multi-output-template
content: |
  ---
  type: tag
  name: {{ name }}-build
  arches: {{ arches | list }}
  ---
  type: tag
  name: {{ name }}-dest
  arches: {{ arches | list }}
  inheritance:
    - name: {{ name }}-build
      priority: 10
  ---
  type: target
  name: {{ name }}
  build-tag: {{ name }}-build
  dest-tag: {{ name }}-dest
