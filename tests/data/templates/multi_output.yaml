# Template that generates multiple YAML documents
---
type: template
name: multi-output-template
content: |
  ---
  type: tag
  name: {{ name }}-build
  description: "Build tag for {{ name }}"
  arches: {{ arches | list }}
  ---
  type: tag
  name: {{ name }}-dest
  description: "Destination tag for {{ name }}"
  arches: {{ arches | list }}
  inheritance:
    - name: {{ name }}-build
      type: tag
      priority: 10
  ---
  type: target
  name: {{ name }}
  build-tag: {{ name }}-build
  dest-tag: {{ name }}-dest
